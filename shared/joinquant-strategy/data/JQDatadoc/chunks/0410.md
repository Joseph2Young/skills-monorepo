> Path: 期货 / 指定日期的期货列表数据

### 指定日期的期货列表数据

```python
get_future_contracts(underlying_symbol, date)
```

**描述**

- 获取某期货品种在指定日期下的可交易合约标的列表
  

**参数**

- underlying\_symbol: 期货合约品种，如 'AU'(白银)
- date：指定日期，默认为None，不指定时返回当前日期下可交易的合约标的列表

  

**返回结果**

- 某一期货品种在指定日期下的可交易合约标的列表
